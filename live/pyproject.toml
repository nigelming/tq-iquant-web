[project]
name = "live"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.7"
dependencies = [
 "pyyaml",
 "tq-iquant-shared",
]
[tool.uv.sources]
tq-iquant-shared = { path = "../shared" }
