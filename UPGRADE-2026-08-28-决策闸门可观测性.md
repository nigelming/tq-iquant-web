# 升级说明：决策闸门可观测性（2026-08-28）

对应提交：`237f935 feat(observability): 决策闸门逐事件落库，风控调参可观测全链路`

## 一、本次升级做了什么

为风控参数调优提供全链路可观测能力。**纯观测，零决策逻辑改动**——所有闸门的拦截/拒单/缩量/熔断行为与升级前完全一致，只是新增了逐事件记录。

信号 → 成成全链路共 **28 个闸门**逐事件落库，覆盖 6 层：

| 层 | 闸门 |
|---|---|
| 策略风控 strategy_risk | 止损 / 止盈 / 移动止损（触发） |
| 组合风控 portfolio_risk | 最大回撤熔断 / 日内亏损暂停（暂停）、熔断恢复、熔断期剥买单 |
| 信号闸门 signal_gate | 停牌无价格、从策略主仓限制、已持仓不再开仓、持股数达上限、减仓量过小、加仓阈值未达、加仓次数超限、缺风控配置 |
| 资金闸门 capital_gate | 开/加仓不足 1 手、资金不足拒单、资金不足缩量 |
| T+1 | 可卖钳量 / 可卖不足 |
| 实盘闸门 live_gate | 收盘后拦单、非交易时段拦单、重复单跳过、在途单跳过、桥离线拒单、桥业务拒单、发单兜底失败 |

每条事件记录：闸门、层、动作、组合/策略/股票代码、bar 时间、参数名与阈值、实际值、请求量与最终量、原因消息。

## 二、改动了哪些东西

**后端（main/）**
- 新增 `core/engine/decision.py`：DecisionRecorder（缓冲采集器）/ DecisionEvent / summarize_decisions（聚合）。默认 NULL_RECORDER 空对象，未接线处零侵入。
- 新增 `core/models/decision_event.py` + 迁移 `e5f6a7b8c9d0`：两张表 `backtest_decision_events`、`live_decision_events`（FK 级联删除 + 各 3 索引）。
- 埋点：portfolio / execution_engine / risk_manager / account（回测+实盘共用层）+ live_engine 实盘专属 7 闸门。
- API：回测详情新增 `decision_summary`（聚合）与 `decisions`（逐笔）；实盘新增 `GET /api/live/sessions/{id}/decisions`；实盘 SSE 新增 `decision` 事件类型。

**前端（web/）**
- 回测详情页新增「风控与拦截统计」面板：按触发次数降序的聚合表，点行展开该闸门逐笔明细。
- 实盘工作台新增「决策/拦截」页签：聚合统计表 + SSE 实时事件流 + 历史回填。
- `liveEvents.ts`：28 闸门 / 6 层 / 8 动作中文映射；SSE 事件流里紫色标签显示。
- `style.css`：补 badge-red/orange/teal/purple（此前模板引用但未定义）。

## 三、部署步骤

> 单机部署，已在本机（2026-08-28 收盘后）完成。以下供以后环境重放。

1. **停服务**：`./manage.ps1 stop` 停 core；桥在 iQuant 客户端内手动停止。**切勿盘中执行**（dev.db 写入 + 桥在跑）。
2. **拉代码**：`git pull`（需 ≥ `237f935`）。
3. **数据库迁移**（在 `main/`）：
   ```powershell
   uv run alembic upgrade head    # 建两张 decision 表（幂等，已应用则无操作）
   uv run alembic current          # 应显示 e5f6a7b8c9d0 (head)
   ```
4. **前端构建**（在 `web/`）：`npm run build`（生产期由 FastAPI 托管 `dist/`）。
5. **启动**：`./manage.ps1 start`（或 restart）；桥在 iQuant 客户端内重新加载策略。

**回滚**：`uv run alembic downgrade -1` 删两表 → `git reset` 回旧版本 → restart。旧版本代码遇到新表无影响（表未被引用），可不回滚数据只回滚代码。

## 四、升级后验证

本次已完成（2026-08-28 收盘后）：

| 项 | 结果 |
|---|---|
| 停服全量测试 | 后端 580 passed / 前端 101 passed |
| 端到端回测 | 「决策观测验证-收盘后」（record 5）：46,645 事件落库，8 类闸门聚合正确 |
| API | `/api/live/sessions/{id}/decisions` 正常；回测详情带 decision_summary/decisions |

手验入口：
- **回测**：打开回测详情 → 「风控与拦截统计」→ 点行下钻逐笔（含阈值/实际/请求·实发量/原因）。
- **实盘**：盘中起 session → 工作台「决策/拦截」页签看 SSE 实时事件 + 收盘后重进页面看历史回填。

## 五、注意事项

1. **事件量较大**：trailing_stop（进入移动止损状态后每根 bar 记录）与 t1_insufficient（持仓期间每 bar 尝试卖出被 T+1 拦）占事件量绝对多数——1.5 个月 1m 回测即 4.6 万行。**调参看聚合表（decision_summary）为主**，逐笔仅用于定位个案。若长期跑后嫌表肥，可改为 trailing_stop 仅在止损位上移时记录（观察面改动，另行确认）。
2. **旧记录无数据**：升级前的回测记录/实盘 session 没有决策事件，面板显示为空属预期。
3. **实盘 SSE 手验待做**：「决策/拦截」页签的实盘实时流尚未在真实 session 中人工核验（前端逻辑已由 vitest 覆盖），下一个交易日跑模拟盘时顺带确认。
4. **F9 印花税仍为 0**：`/deals` 回填的 commission 含佣金，印花税字段 iQuant 未提供（与本升级无关，遗留项）。
